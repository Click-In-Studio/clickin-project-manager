{
  "id": "clickin-memory",
  "name": "Click-In Memory",
  "description": "Click-In 团队记忆注入 + MCP 写工具确认门（Phase 4 v1）",
  "version": "0.1.0",
  "contracts": {
    "agentToolResultMiddleware": ["openclaw"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mcpUrl": {
        "type": "string",
        "description": "clickin MCP server 地址（annotations 拉取 / 注入取件 / episodic 上报都走它）",
        "default": "http://127.0.0.1:3101/mcp"
      },
      "approvalEnabled": {
        "type": "boolean",
        "description": "对非只读 clickin__* 工具挂 requireApproval 确认门",
        "default": true
      }
    }
  }
}
