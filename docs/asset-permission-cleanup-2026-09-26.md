# #427 资产权限盘点与清洗记录

#708 已合并。用户已明确授权 dev、prod 按既定边界盘点及清洗；此次不需要再次申请写入授权。

## 分环境盘点

| 指标 | dev | prod |
| --- | ---: | ---: |
| 旧资产 meta@view 通配能力票 | 12 | 13 |
| 其中非 owner 且无显式全库读取票 | 6 | 7 |
| 显式 publication@view 通配读取票 | 5 | 5 |
| 角色 meta@view / file@view 区间 | 各 280 | 各 280 |
| 不公开但列出的资产 | 2 | 14 |
| 有效悬空资产实例授权 | 20 | 20 |
| 单向旧发布权但没有 grants@edit | 0 | 0 |
| 缺失资产壳 / 资产壳悬空 | 0 / 0 | 0 / 0 |

两边挂载均为评论 1、嵌入 15、场次 15，部门分享为 0。11 个项目保存的公开及上传者删除、发布、分享策略均为 on。旧通配票仍是当前代码读取条件的一部分，直接撤销会失去原有访问；直接重解释则会扩大私有可见性。因此此次不转换有效票、区间、模板或仅列出资产的公开状态，#427 核心对齐仍待兼容代码及逐受众差异验证。

仅列出资产分布：`monc5bw51` 两边各 1；`mt07gsia1` dev 1、prod 13；均无挂载。角色 meta@view 区间按项目为 17、19、35、11、20、20、12、35、37、37、37（共 280）；完整聚合盘点结果分别留在执行机，不提交用户明细。

## 本次可执行清洗

两边候选均来自项目 `modlvhs01` 下已不存在的 `ast_mt5tbexj1`、`ast_mt5tbjqx4`，各 10 条创建者授权，2026-08-23 创建，均无审批关联。资产和对应 node 均不存在，撤销这些行不改变任何现存内容的可达性。

迁移 `20260926041043_revoke_orphan_asset_grants.sql` 仅撤销 2026-09-26 04:07 UTC 盘点开始前创建、尚有效、资产及 node 均不存在的实例授权，设置 `is_revoked=true, revoked_reason=manual`。保留原行及来源、期限、审批信息；不处理通配票、已有实体、已撤销/过期行及盘点后新增行。删除资产的应用写点同步修复（#709），避免继续留下有效审计行。

## 执行与补偿

脚本：`scripts/admin/run-asset-orphan-cleanup.sh`，快照与补偿 SQL 在同目录。以 postgres 用户执行，先 dev 后 prod；每个环境独立核对 release、数量与摘要。

1. `prepare`：完整 pg_dump 及可读取目录清单；完整候选行 JSON 快照；非目标 grant、三类区间、策略、节点、资产的合并摘要；脚本 SHA-256 清单。目录 700、文件 600，仅 postgres 可读。
2. `rehearse`：同一事务执行清洗和定向补偿，逐行比较原始快照后整体回滚，不留下迁移记账。
3. `apply`：dbmate 只加载本次 migration。3 秒锁超时、15 秒语句超时；持锁后核对完整目标行摘要，不一致则事务回滚。迁移由 dbmate 写入 schema_migrations。
4. 核对仅目标行的两个字段改变、剩余候选为 0、非目标摘要不变及迁移记账。任何失败停止后续环境。
5. 必须补偿时执行 `restore`：仅在目标行仍精确等于本次清洗后的状态、实体仍不存在时还原两个字段；后续修改导致整笔拒绝。保留 schema_migrations 历史，不执行不可逆 down，不直接恢复整库覆盖后续业务写入。

## 验证与执行状态

- 全量测试：356 文件通过，3,682 用例通过；149 跳过（本地已执行迁移导致该迁移的 2 个 hook 依赖测试跳过，另在旧结构隔离库验证通过）。
- 隔离旧结构库：hook 无条件造数，真实 dbmate 升级后迁移及删除回归 5 用例全部通过。
- 补偿测试验证原样恢复、非目标不变及后续修改时拒绝；错误快照摘要拒绝迁移。
- 类型检查、schema 三方一致性检查通过；lint 0 错误、7 个既有警告。恢复旧删除实现的反证测试按预期失败，随后还原已提交修复。
- #710 首次 AI review 已逐项核对：更新日志已补齐；明确 CI/CD 无摘要时按保守谓词迁移，人工线上 runner 必须传摘要；沿用 manual 以兼容当前约束及 N-1，当前审计字段不能区分删除触发与逐条撤销，本次历史清洗由快照及迁移回执识别。均为补充说明，无 major refactor，不重复请求 review。
- dev、prod 均完成清洗→补偿→逐行还原→ROLLBACK 演练。dev 额外验证错误摘要被拒绝、演练无迁移记账。随后分环境正式执行，回执如下。

## 线上执行回执（已完成）

| 环境 | 清洗时 release | 完成时间（北京时间） | 目标 / 已撤销 / 剩余有效悬空 | 非目标摘要 | 登录入口 |
| --- | --- | --- | --- | --- | --- |
| dev | `418-38bdab4f2a6da4a6a50e419cff` | 2026-09-26 12:31:04 | 20 / 20 / 0 | 不变 | 3000 端口 HTTP 307 |
| prod | `411-5856b63aa6e3bbf21e884aef98` | 2026-09-26 12:33:16 | 20 / 20 / 0 | 不变 | 实际 3001 端口 HTTP 200 |

两边 schema_migrations 均已记录 `20260926041043`，执行前为 3 支，执行后 4 支。dbmate 用时 dev 10.29ms、prod 12.30ms。各 20 行逐行比较通过，仅 `is_revoked`、`revoked_reason` 改变；后续完整只读盘点确认通配票、三类区间、策略、节点公开/列出设置及挂载统计不变，有效悬空授权归零。

备份均保留在各服务器：
`/var/www/production-manager/shared/backups/asset-permission-cleanup-20260926/`

- `pre-cleanup.pgdump`：dev 7,400,291 字节，prod 11,179,319 字节；pg_restore --list 通过。
- `before.json` / `after.json`：原始目标行、目标摘要及非目标摘要；原始业务明细不提交仓库。
- `reviewed-files.sha256`、`migration.log`、`rehearsal.log`、`verified-count.txt`、`applied-version.txt`、`completed-at.txt`：文件与执行回执。dev 另保留错误摘要拒绝日志及演练前的脚本摘要。
- 目录权限 700、文件权限 600，属主 postgres；定向补偿脚本留在同目录。备份未自动删除。

线上执行的 migration SHA-256：
`b029a561bc096e55d5ffbdd637af9aad9e54cc6ab9c490f41ecf62fef6336dab`

此次只执行已授权的数据清洗，没有部署应用代码。防复发修复在 #710 中，待合并并按各环境发布流程部署。旧代码仍可能在新的资产删除后留下授权，固定盘点截止日期的本次 migration 不会处理这些新行；不重跑或修改已在线执行的 migration。

#427 仍保持开放：有效能力票、区间与默认模板的兼容转换，以及仅列出资产受众差异验证，必须与核心读取规则一起完成，不能仅凭此次悬空行清洗宣告全部对齐。
